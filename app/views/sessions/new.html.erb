<div class="row">
    <%= form_for(:session, url: login_path, html: {id: 'newSession'}) do |f| %>

      <%= f.label :email %>
      <%= f.email_field :email%>

      <%= f.label :password %>
      <%= f.password_field :password%>

      <%= f.submit "Login" %>
    <% end %>
</div>

